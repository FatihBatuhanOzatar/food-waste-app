// Placeholder — to be implemented in the auth sprint.
// TODO: Implement AppUser model per ARCHITECTURE.md model layer rules.
// Required: fromJson, toJson, copyWith, immutable fields, Equatable.

/// Placeholder for the authenticated user model.
///
/// Will represent both end-users and business owners, distinguished
/// by a role field backed by the `profiles` table in Supabase.
class AppUser {
  /// Creates an [AppUser].
  const AppUser();
}
