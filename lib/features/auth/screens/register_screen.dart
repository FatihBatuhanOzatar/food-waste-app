// Placeholder — to be implemented in the auth sprint.
// TODO: Implement RegisterScreen per FEATURE_SPECS.md (US-02).

import 'package:flutter/material.dart';

/// Placeholder for the registration screen.
///
/// Will display name, e-mail, and password fields backed by
/// [AuthRepository.signUp].
class RegisterScreen extends StatelessWidget {
  /// Creates the [RegisterScreen].
  const RegisterScreen({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
