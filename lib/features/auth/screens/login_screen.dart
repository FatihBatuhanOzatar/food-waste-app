// Placeholder — to be implemented in the auth sprint.
// TODO: Implement LoginScreen per FEATURE_SPECS.md user stories (US-01, US-02).

import 'package:flutter/material.dart';

/// Placeholder for the login screen.
///
/// Will display e-mail/password fields, a sign-in button,
/// and a link to the registration screen.
class LoginScreen extends StatelessWidget {
  /// Creates the [LoginScreen].
  const LoginScreen({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
