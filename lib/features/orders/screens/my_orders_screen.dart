// Placeholder — to be implemented in the orders sprint.
// TODO: Implement MyOrdersScreen — paginated list of user's past and active orders.

import 'package:flutter/material.dart';

/// Placeholder for the user's order history screen.
class MyOrdersScreen extends StatelessWidget {
  /// Creates the [MyOrdersScreen].
  const MyOrdersScreen({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
