// Placeholder — to be implemented in the orders sprint.
// TODO: Implement ReservationScreen — confirm quantity, show price, submit reservation.
// Reminder: payment is offline (user pays at business). No payment code here.

import 'package:flutter/material.dart';

/// Placeholder for the reservation creation screen.
class ReservationScreen extends StatelessWidget {
  /// Creates the [ReservationScreen].
  const ReservationScreen({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
