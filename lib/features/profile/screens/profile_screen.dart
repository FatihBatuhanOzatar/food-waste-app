// Placeholder — to be implemented in the profile sprint.
// TODO: Implement ProfileScreen — user info, settings, sign-out, link to impact dashboard.

import 'package:flutter/material.dart';

/// Placeholder for the user profile and settings screen.
class ProfileScreen extends StatelessWidget {
  /// Creates the [ProfileScreen].
  const ProfileScreen({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
