// Placeholder — to be implemented in the products sprint.
// TODO: Implement ProductCard widget — shows name, price, discount %, business name.
// Must use AppColors, AppSpacing, AppTypography — no inline styles.

import 'package:flutter/material.dart';

/// Placeholder for the product list card widget.
class ProductCard extends StatelessWidget {
  /// Creates a [ProductCard].
  const ProductCard({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
