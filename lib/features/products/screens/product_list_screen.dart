// Placeholder — to be implemented in the products sprint.
// TODO: Implement ProductListScreen — list + map tab layout showing nearby products.

import 'package:flutter/material.dart';

/// Placeholder for the product discovery / home screen.
class ProductListScreen extends StatelessWidget {
  /// Creates the [ProductListScreen].
  const ProductListScreen({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
