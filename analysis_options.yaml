# Analysis options for the Food Waste App.
# Strict lints to keep the codebase clean and consistent.
# See: https://dart.dev/guides/language/analysis-options

include: package:flutter_lints/flutter.yaml

analyzer:
  # Enable the custom_lint plugin for riverpod_lint rules.
  # Run `dart run custom_lint` for a full lint report.
  plugins:
    - custom_lint

  exclude:
    # Exclude code-generated files from analysis.
    - '**/*.g.dart'
    - '**/*.freezed.dart'

linter:
  rules:
    # --- Error prevention ---
    avoid_print: true
    avoid_slow_async_io: true
    cancel_subscriptions: true
    close_sinks: true

    # --- Code style ---
    prefer_const_constructors: true
    prefer_const_constructors_in_immutables: true
    prefer_const_declarations: true
    prefer_const_literals_to_create_immutables: true
    prefer_final_fields: true
    prefer_final_locals: true
    prefer_single_quotes: true
    require_trailing_commas: true
    sort_constructors_first: true
    unnecessary_this: false

    # --- Design ---
    avoid_classes_with_only_static_members: false
    avoid_unnecessary_containers: true
    sized_box_for_whitespace: true
    use_full_hex_values_for_flutter_colors: true
    use_key_in_widget_constructors: true
